const month=3
switch (month) {
    case 1:
        console.log(`month ${month} ,january`);
        break;
    case 2:
        console.log(`month ${month} ,feb`);
        break;
    case 3:
        console.log(`month ${month} ,march`);
        break;

    default:
        console.log(`default month`);
        
        break;
}